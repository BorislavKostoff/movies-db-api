import React from 'react'
import spinner from '../img/spinner.gif'

const Spinner = () => {
  return (
    <img
      className='spinner-img-style'
      src={spinner}
      alt='spinner'
    />
  )
}

export default Spinner
